apiVersion: apps/v1
kind: Deployment
metadata:
  name: vectordb
  labels:
     app: vectordb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vectordb
  template:
    metadata:
      labels:
        app: vectordb
    spec:
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      containers:
      - name: vectordb-manager
        image: {{ .Values.image.repository }}vectordb:{{ .Values.image.tag }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: 9001
          hostPort: 9001
        env:
        - name: http_proxy
          value: {{ .Values.proxy.http }}
        - name: https_proxy
          value: {{ .Values.proxy.https }}
        - name: VECTORDB_SERVICE_HOST_IP
          value: {{ .Values.service.vectordb_host }}
        volumeMounts:
        - mountPath: /etc/localtime
          name: localtime
          readOnly: true
        - mountPath: /etc/timezone
          name: timezone
          readOnly: true
        - mountPath: /root/.cache/huggingface
          name: huggingface
        - mountPath: /home/data
          name: vectordb-data
      - name: chromadb
        image: chromadb/chroma:latest
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: 8000
          hostPort: 8000
        env:
        - name: ANONYMIZED_TELEMETRY
          value: "{{ .Values.chroma.anonymized_telemetry }}"
        - name: ALLOW_RESET
          value: "{{ .Values.chroma.allow_reset }}"
        - name: IS_PERSISTENT
          value: "{{ .Values.chroma.is_persistent }}"
        - name: PERSIST_DIRECTORY
          value: {{ .Values.chroma.persist_directory }}
        volumeMounts:
        - mountPath: /chroma/chroma
          name: chroma-data
      - name: vdms
        image: intellabs/vdms:latest
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: 55555
          hostPort: 55555
      volumes:
      - name: localtime
        hostPath:
          path: /etc/localtime
          type: File
      - name: timezone
        hostPath:
          path: /etc/timezone
          type: File
      - name: huggingface
        hostPath: 
          path: /home/{{ .Values.service.user }}/.cache/huggingface
          type: DirectoryOrCreate
      - name: vectordb-data
        hostPath:
          path: /var/data/vectordb
          type: DirectoryOrCreate
      - name: chroma-data
        hostPath:
          path: /var/data/chroma
          type: DirectoryOrCreate

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-interface
  labels:
     app: user-interface
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-interface
  template:
    metadata:
      labels:
        app: user-interface
    spec:
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      initContainers:
      - name: wait-video-ingestor
        image: busybox
        command: ["sh", "-c", "sleep 10; while [ ! -f /mnt/video-ready ]; do sleep 1; echo waiting for /mnt/video-ready...; done"]
        volumeMounts:
        - mountPath: /mnt
          name: mnt
          readOnly: true
      containers:
      - name: user-interface
        image: {{ .Values.image.repository }}user-interface:{{ .Values.image.tag }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
        - containerPort: 50055
          hostPort: 50055
        env:
        - name: http_proxy
          value: {{ .Values.proxy.http }}
        - name: https_proxy
          value: {{ .Values.proxy.https }}
        - name: VECTORDB_SERVICE_HOST_IP
          value: {{ .Values.service.vectordb_host }}
        - name: HUGGINGFACEHUB_API_TOKEN
          value: {{ .Values.huggingface.api_token }}
        volumeMounts:
        - mountPath: /etc/localtime
          name: localtime
          readOnly: true
        - mountPath: /etc/timezone
          name: timezone
          readOnly: true
        - mountPath: /root/.cache/huggingface
          name: huggingface
      volumes:
      - name: localtime
        hostPath:
          path: /etc/localtime
          type: File
      - name: timezone
        hostPath:
          path: /etc/timezone
          type: File
      - name: huggingface
        hostPath: 
          path: /home/{{ .Values.service.user }}/.cache/huggingface
          type: DirectoryOrCreate
      - name: mnt
        hostPath:
          path: /mnt
          type: DirectoryOrCreate

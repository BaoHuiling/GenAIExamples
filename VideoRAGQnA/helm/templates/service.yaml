apiVersion: v1
kind: Service
metadata:
  name: {{ include "video-rag-qna.fullname" . }}
  labels:
    {{- include "video-rag-qna.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "video-rag-qna.selectorLabels" . | nindent 4 }}
